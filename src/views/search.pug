extends base
include mixins/video

block content 
    form(method="GET")
        input(type="text", placeholder="Serch by Title", name="keyword")
        input(type="submit", value="Search now")

    each video in videos
        +video(video)